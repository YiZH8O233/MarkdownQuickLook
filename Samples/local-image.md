# Local Image

The preview extension should only load local image paths relative to this Markdown file.

![Local sample image](images/local-sample.ppm)

If the image renderer is text-only in this version, the preview should still show a readable image label.
